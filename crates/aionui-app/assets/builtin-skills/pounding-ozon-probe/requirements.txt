# ══════════════════════════════════════════════════════════════
# pounding-ozon-probe 依赖清单
# ══════════════════════════════════════════════════════════════
# 系统要求:
#   Python >= 3.12（必须，二进制模块编译版本）
#   Google Chrome（必须，CDP 浏览器抓取依赖）
#   pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# --- 核心依赖 ---
requests>=2.28                 # HTTP 请求（Worker API / Ozon API）
websocket-client>=1.6.0        # Chrome DevTools Protocol 通信
Pillow>=10.0.0                 # 图片处理（1688 图搜 / 图片预处理）

# --- 可选依赖（错误追踪） ---
# sentry-sdk>=1.40.0           # Sentry 错误追踪（可选，不装不影响功能）
